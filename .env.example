# Copy this file to .env and fill in real values before running docker compose.
# NEVER commit .env to git.

# Payload CMS — generate with: openssl rand -hex 32
PAYLOAD_SECRET=

# MySQL — used by Perfex CRM
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=
