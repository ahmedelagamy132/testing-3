# Dependencies (reinstalled inside Docker)
node_modules

# Build output (rebuilt inside Docker)
.next

# Local form-submission database (mounted as a Docker volume in production)
miduva.db
miduva.db-shm
miduva.db-wal
.puck-media

# Secrets
.env
.env.local
.env.*.local

# Dev tooling
.git
.gitignore
.claude
*.md
README*
